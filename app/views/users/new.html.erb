<h1>注册，需邮件验证。</h1>
<% @user.password = @user.password_confirmation = nil %>

<%= error_messages_for :user %>
<%= form_for :user, :url => users_path do |f| -%>
<p><%= label_tag '用户名' %><br/>
<%= f.text_field :login %> *姓名的英文拼写，保持与邮箱前一致。</p>

<p><%= label_tag '邮箱' %><br/>
<%= f.text_field :email %> *填写真实的邮件地址，后缀必须为talkweb.com.cn。</p>

<p><%= label_tag '密码' %><br/>
<%= f.password_field :password %>* sha2加密，不能找回。</p>

<p><%= label_tag 'password_confirmation', '重复密码' %><br/>
<%= f.password_field :password_confirmation %></p>

<p><%= submit_tag '注册' %></p>
<% end -%>
